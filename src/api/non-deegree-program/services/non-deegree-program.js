'use strict';

/**
 * non-deegree-program service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::non-deegree-program.non-deegree-program');
